# gtm-local-storage-source-data-variable-template
A variable template for Google Tag Manager (GTM) that retrieves the value from the local storage key within the "source_data" object with the matching name.
